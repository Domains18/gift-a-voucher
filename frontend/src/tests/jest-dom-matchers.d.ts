declare module '@testing-library/jest-dom/matchers' {
  import { TestingLibraryMatchers } from '@testing-library/jest-dom/matchers';
  const matchers: TestingLibraryMatchers<any, void>;
  export default matchers;
}
